# Safety and capability boundary

The release is a local static web runtime. It requests no camera, microphone, location, WebXR, Bluetooth, contacts, accounts or network-writing capability. It has no arbitrary code execution inside world processes, no LLM, no Synthetic Network and no Human Internet bridge. Embodiment actuators affect only synthetic physical bodies. GitHub Pages only distributes static files.
