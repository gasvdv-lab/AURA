# Genesis

v0.2 genesis creates infrastructure, not AURA: an empty objective world, material laws, zero or explicitly seeded resources/bodies, tick zero, seeded random state and empty Observatory. The demonstration body in `app.js` is a laboratory fixture, not a living entity or high-level game object.
