import {strict as assert} from 'node:assert';import {createBelief} from '../helpers-belief.mjs';const x=createBelief();x.addTarget('target');const state=x.perception.observe({embodimentId:x.id});x.memory.record(state);x.belief.ingestPerception(state);assert.equal(x.memory.traces.size,1);assert.equal(x.perception.observations.length,1);assert.equal(x.belief.beliefs.size,4);assert.equal(x.world.bodies.size,15);assert.ok(x.world.observatory.verify());assert.equal(x.sensorimotor.trials.length,0);
