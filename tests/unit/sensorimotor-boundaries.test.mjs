import {strict as assert} from 'node:assert';import {createSensorimotor} from '../helpers-sensorimotor.mjs';const x=createSensorimotor({gravity:{x:0,y:0,z:0}}),a=x.entity.actuators[0].id;assert.throws(()=>x.sensorimotor.runTrial({embodimentId:x.id,actuatorId:a,command:2,steps:1}));assert.throws(()=>x.sensorimotor.runTrial({embodimentId:x.id,actuatorId:a,command:.1,steps:0}));for(const skill of ['walk','stand','grasp','explore','chooseAction'])assert.equal(typeof x.sensorimotor[skill],'undefined');
