import {strict as assert} from 'node:assert';import {createSensorimotor} from '../helpers-sensorimotor.mjs';const x=createSensorimotor();const s=x.sensorimotor.sample(x.id);assert.equal(s.parts.length,14);assert.ok(Number.isFinite(s.centerOfMass.y));assert.equal(s.contactCount,0);for(const forbidden of ['pain','balance','meaning','goal','success','failure','experience'])assert.equal(JSON.stringify(s).toLowerCase().includes(forbidden),false);
