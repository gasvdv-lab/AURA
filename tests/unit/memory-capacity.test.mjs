import {strict as assert} from 'node:assert';import {createMemory} from '../helpers-memory.mjs';const x=createMemory({memory:{traceCapacity:2}});x.addTarget('target');for(let i=0;i<3;i++)x.memory.record(x.perception.observe({embodimentId:x.id}));assert.equal(x.memory.traces.size,2);assert.equal(x.memory.traces.has('trace-1'),false);assert.ok(x.world.observatory.records.some(e=>e.type==='memory.forgotten'&&e.payload.reason==='sensory-capacity'));
