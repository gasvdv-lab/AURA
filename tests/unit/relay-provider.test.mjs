import {strict as assert} from 'node:assert';import {RelayProvider} from '../../src/foundation-model/providers.js';let captured;const fetchImpl=async(url,options)=>{captured={url,options};return {ok:true,json:async()=>({id:'r1',model:'test-model',output_text:'antwoord'})};};const p=new RelayProvider({endpoint:'https://relay.example/v1/aura/respond',model:'allowed-model',fetchImpl});const result=await p.generate({instructions:'bounded',input:'vraag',context:{schema:'aura-controlled-context-v1'}});assert.equal(result.text,'antwoord');assert.equal(captured.options.headers.authorization,undefined);const body=JSON.parse(captured.options.body);assert.equal(body.store,false);assert.deepEqual(body.tools,[]);assert.equal(body.model,'allowed-model');assert.throws(()=>new RelayProvider({endpoint:'http://unsafe.example/x',model:'x'}));
