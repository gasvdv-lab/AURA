import {strict as assert} from 'node:assert';import {callGemini,callGroq} from '../../server/relay-example.mjs';const incoming={instructions:'alleen context',input:'vraag',context:{schema:'aura-controlled-context-v1'}};let a;const g=await callGemini({apiKey:'geheim',model:'gemini-test',incoming,fetchImpl:async(url,options)=>{a={url,options};return {ok:true,json:async()=>({candidates:[{content:{parts:[{text:'gemini antwoord'}]}}]})};}});assert.equal(g.output_text,'gemini antwoord');assert.equal(a.options.headers['x-goog-api-key'],'geheim');assert.match(a.url,/generativelanguage\.googleapis\.com/);assert.equal(JSON.parse(a.options.body).generationConfig.maxOutputTokens,400);let b;const q=await callGroq({apiKey:'geheim',model:'groq-test',incoming,fetchImpl:async(url,options)=>{b={url,options};return {ok:true,json:async()=>({id:'g1',model:'groq-test',choices:[{message:{content:'groq antwoord'}}]})};}});assert.equal(q.output_text,'groq antwoord');assert.equal(b.options.headers.authorization,'Bearer geheim');assert.equal(JSON.parse(b.options.body).max_completion_tokens,400);assert.equal(JSON.parse(b.options.body).tools,undefined);
