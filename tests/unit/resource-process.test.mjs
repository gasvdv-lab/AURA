import {assert,createDefaultWorld} from '../helpers.mjs';const w=createDefaultWorld();w.addResource('raw',10);w.transformResource({inputType:'raw',outputType:'refined',amount:4,efficiency:.75});assert.equal(w.resources.get('raw'),6);assert.equal(w.resources.get('refined'),3);assert.equal(w.totalResource(),9);assert.throws(()=>w.transformResource({inputType:'raw',outputType:'x',amount:99}));
