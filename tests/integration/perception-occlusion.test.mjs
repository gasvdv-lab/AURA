import {strict as assert} from 'node:assert';import {createPerception} from '../helpers-perception.mjs';import {vec3} from '../helpers.mjs';const x=createPerception({perception:{range:10,noise:0}});x.addTarget('blocker',vec3(0,5.22,-2),{radius:.5});x.addTarget('hidden',vec3(0,5.22,-4),{radius:.4});const state=x.perception.observe({embodimentId:x.id});assert.equal(state.items.length,1);assert.ok(state.items[0].distance<3);
