import {strict as assert} from 'node:assert';import {createPerception} from '../helpers-perception.mjs';import {vec3} from '../helpers.mjs';function run(){const x=createPerception({perception:{seed:44,noise:.03}});x.addTarget('z',vec3(1,5,-3));x.addTarget('a',vec3(-1,4,-2));return [x.perception.observe({embodimentId:x.id}),x.perception.observe({embodimentId:x.id}),x.perception.snapshot(),x.world.observatory.snapshot()];}assert.deepEqual(run(),run());
