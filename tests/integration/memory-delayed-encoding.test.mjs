import {strict as assert} from 'node:assert';import {createMemory} from '../helpers-memory.mjs';const x=createMemory();x.addTarget('target');const old=x.perception.observe({embodimentId:x.id});x.kernel.step(60);x.memory.advance(x.world.tick);const trace=x.memory.record(old);assert.equal(trace.sourceTick,0);assert.equal(trace.createdTick,60);assert.equal(trace.encodingDelay,60);assert.equal(x.memory.lastTick,60);assert.equal(x.memory.traces.size,1);assert.ok(x.world.observatory.records.some(e=>e.type==='memory.trace-recorded'));
