import {strict as assert} from 'node:assert';import {createEmbodiment} from '../helpers-embodiment.mjs';import {WorldKernel} from '../../src/core/world/world-kernel.js';import {EmbodimentKernel} from '../../src/embodiment/embodiment-kernel.js';const a=createEmbodiment({fixedDelta:.01});a.kernel.commandActuator(a.id,a.entity.actuators[5].id,-.4);a.kernel.step(9);const w=WorldKernel.restore(a.world.snapshot()),k=EmbodimentKernel.restore(w,a.kernel.snapshot());a.kernel.step(21);k.step(21);assert.deepEqual(w.snapshot(),a.world.snapshot());assert.deepEqual(k.snapshot(),a.kernel.snapshot());
