import {strict as assert} from 'node:assert';import {createFoundation} from '../helpers-foundation.mjs';const x=createFoundation();await assert.rejects(()=>x.bridge.respond({context:{schema:'wrong'},input:''}));await assert.rejects(()=>x.bridge.respond({context:x.compiler.compile(),input:'x'.repeat(1001)}));assert.equal(typeof x.bridge.execute,'undefined');assert.equal(typeof x.bridge.useTool,'undefined');assert.equal(x.world.bodies.size,14);
