import {access,readFile} from 'node:fs/promises';const required=['index.html','README.md','ROADMAP.md','TESTING.md','MODULES.md','AURA_CONSTITUTION.md','EXPERIMENTS.md','docs/WORLD_LAWS.md','docs/OBSERVATORY.md','.github/workflows/validate.yml'];for(const p of required)await access(p);const link='https://gasvdv-lab.github.io/AURA/';for(const p of ['README.md','TESTING.md']){const s=await readFile(p,'utf8');if(!s.includes(link))throw new Error(`${p} lacks fixed live link`);}const banned=['WebXR','getUserMedia','geolocation','RTCPeerConnection'];for(const p of ['src/app.js','src/core/world/world-kernel.js']){const s=await readFile(p,'utf8');for(const token of banned)if(s.includes(token))throw new Error(`Forbidden runtime capability ${token} in ${p}`);}console.log('AURA v0.2.0 release validation: PASS');console.log(`Validated ${required.length} required paths, cumulative live links, and runtime boundaries.`);
