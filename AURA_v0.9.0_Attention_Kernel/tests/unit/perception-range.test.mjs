import {strict as assert} from 'node:assert';import {createPerception} from '../helpers-perception.mjs';import {vec3} from '../helpers.mjs';const x=createPerception({perception:{range:2,noise:0}});x.addTarget('near',vec3(0,5,-1));x.addTarget('far',vec3(0,5,-8));const state=x.perception.observe({embodimentId:x.id});assert.equal(state.items.length,1);assert.ok(state.items[0].distance<=2);assert.throws(()=>new x.perception.constructor(x.world,x.kernel,{range:0}));
