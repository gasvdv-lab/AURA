import {assert,createDefaultWorld,vec3,body} from '../helpers.mjs';let w=createDefaultWorld({fixedDelta:1,gravity:vec3()});let id=body(w,{mass:2});w.applyForce(id,vec3(4,0,0));w.step();assert.deepEqual(w.bodies.get(id).velocity,vec3(2,0,0));w=createDefaultWorld({fixedDelta:.5});id=body(w);w.step();assert.equal(w.bodies.get(id).velocity.y,-4.905);assert.equal(w.bodies.get(id).position.y,-2.4525);
