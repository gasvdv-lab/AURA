import {strict as assert} from 'node:assert';import {createMemory} from '../helpers-memory.mjs';const x=createMemory({memory:{traceDecay:.1,forgetThreshold:.15}});x.addTarget('target');x.memory.record(x.perception.observe({embodimentId:x.id}));x.memory.advance(5);assert.equal(x.memory.traces.get('trace-1').strength,.5);x.memory.advance(9);assert.equal(x.memory.traces.size,0);assert.ok(x.world.observatory.records.some(e=>e.type==='memory.forgotten'));
