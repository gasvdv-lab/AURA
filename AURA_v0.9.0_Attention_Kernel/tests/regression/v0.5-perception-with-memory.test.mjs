import {strict as assert} from 'node:assert';import {createMemory} from '../helpers-memory.mjs';const x=createMemory();x.addTarget('target');const state=x.perception.observe({embodimentId:x.id});x.memory.record(state);assert.equal(x.perception.observations.length,1);assert.equal(x.world.bodies.size,15);assert.equal(x.sensorimotor.trials.length,0);assert.ok(x.world.observatory.verify());assert.equal(x.memory.traces.size,1);assert.equal(x.memory.memories.size,0);
