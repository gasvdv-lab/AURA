import {strict as assert} from 'node:assert';import {createPerception} from '../helpers-perception.mjs';const x=createPerception();x.addTarget('target');const bodies=x.world.bodies.size,a=x.entity.actuators[0].id;x.sensorimotor.runTrial({embodimentId:x.id,actuatorId:a,command:.1,steps:2});x.perception.observe({embodimentId:x.id});assert.equal(x.world.bodies.size,bodies);assert.equal(x.sensorimotor.trials.length,1);assert.equal(x.perception.observations.length,1);assert.ok(x.world.observatory.verify());assert.equal(x.world.resources.size,0);
