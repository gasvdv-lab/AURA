import {strict as assert} from 'node:assert';import {createEmbodiment} from '../helpers-embodiment.mjs';const {world,kernel,entity}=createEmbodiment();const count=world.bodies.size;assert.equal(count,14);kernel.step(40);assert.equal(world.bodies.size,count);assert.equal(world.resources.size,0);assert.ok(world.observatory.verify());assert.equal(world.observatory.records.filter(e=>e.type==='embodiment.created').length,1);assert.equal(entity.actuators.every(a=>a.command===0),true);
