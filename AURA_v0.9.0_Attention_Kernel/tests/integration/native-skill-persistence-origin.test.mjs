import {strict as assert} from 'node:assert';import {NativeSkillKernel} from '../../src/native-skill/native-skill-kernel.js';import {CapabilityKernel} from '../../src/capability/capability-kernel.js';
let n=new NativeSkillKernel();n.create({id:'arithmetic.multiply'});for(const [inputs,output] of [[[2,3],6],[[4,5],20],[[3,7],21]])n.addExample('arithmetic.multiply',{inputs,output});n.learn('arithmetic.multiply');n=NativeSkillKernel.restore(n.snapshot());const c=new CapabilityKernel();c.propose({id:'arithmetic.multiply'});c.addEvidence('arithmetic.multiply',{sourceType:'self-derived',sourceId:'native-rule',summary:n.get('arithmetic.multiply').selectedRule});for(const [challengeId,inputs,expected] of [['n1',[6,8],48],['n2',[9,4],36]]){const r=n.runChallenge('arithmetic.multiply',{challengeId,inputs});c.recordEvaluation('arithmetic.multiply',{testId:challengeId,passed:r.output===expected,challenge:inputs.join(','),expected:String(expected),observed:String(r.output),evaluationSource:'native-runtime'});}assert.equal(c.available()[0].origin,'aura-learned');
