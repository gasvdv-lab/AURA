import {assert,createDefaultWorld,vec3,body} from '../helpers.mjs';import {WorldKernel} from '../../src/core/world/world-kernel.js';const a=createDefaultWorld({fixedDelta:.05});body(a,{position:vec3(0,10,0),velocity:vec3(1,0,0)});a.step(17);const b=WorldKernel.restore(JSON.parse(JSON.stringify(a.snapshot())));a.step(83);b.step(83);assert.deepEqual(a.snapshot(),b.snapshot());assert.ok(b.observatory.verify());
