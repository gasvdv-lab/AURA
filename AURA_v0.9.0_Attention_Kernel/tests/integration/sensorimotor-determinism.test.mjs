import {strict as assert} from 'node:assert';import {createSensorimotor} from '../helpers-sensorimotor.mjs';function run(){const x=createSensorimotor({fixedDelta:.015});const a=x.entity.actuators[3].id;x.sensorimotor.runTrial({embodimentId:x.id,actuatorId:a,command:-.3,steps:7});x.sensorimotor.runTrial({embodimentId:x.id,actuatorId:a,command:.2,steps:4});return {world:x.world.snapshot(),embodiment:x.kernel.snapshot(),sensorimotor:x.sensorimotor.snapshot()};}assert.deepEqual(run(),run());
