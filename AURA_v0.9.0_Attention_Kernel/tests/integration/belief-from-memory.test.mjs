import {strict as assert} from 'node:assert';import {createBelief} from '../helpers-belief.mjs';const x=createBelief({perception:{noise:0}});x.addTarget('target');x.memory.record(x.perception.observe({embodimentId:x.id}));const recall=x.memory.retrieve({track:'track-1',currentTick:0}),ids=x.belief.ingestRecall(recall,0);assert.equal(ids.length,4);const b=x.belief.getBelief('track-1','distance');assert.equal(b.provenance[0].type,'memory');assert.ok(b.alternatives[0].confidence<=.75);
