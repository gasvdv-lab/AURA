import {createBelief} from './helpers-belief.mjs';import {ContextCompiler} from '../src/foundation-model/context-compiler.js';import {FoundationModelBridge} from '../src/foundation-model/foundation-model-bridge.js';import {DeterministicTestProvider} from '../src/foundation-model/providers.js';
export function createFoundation(){const x=createBelief({perception:{noise:0}}),compiler=new ContextCompiler(),provider=new DeterministicTestProvider(),bridge=new FoundationModelBridge({provider,emit:(type,payload,causeIds=[])=>x.world.emit(type,payload,causeIds)});return {...x,compiler,provider,bridge};}
